export type TokenResponse = {
  code: number;
  msg: string;
  token: string;
};

export type TaskResponse = {
  code: number;
  msg: string;
};

export type ArchiveLink = {
  title: string;
  url: string;
  info: string;
  date: string;
};
